CommonBehavior.Endpoints=tcp -p 17000

# Proxies for required interfaces
AprilTagsProxy = apriltags:tcp -h localhost -p 12555
DifferentialRobotProxy = differentialrobot:tcp -h localhost -p 10004
LaserProxy = laser:tcp -h localhost -p 10003
FullPoseEstimationProxy = fullposeestimation:tcp -h localhost -p 10696
RoomDetectionProxy = roomdetection:tcp -h localhost -p  18046
CameraRGBDSimpleProxy = camerargbdsimple:tcp -h localhost -p 10095


Ice.Warn.Connections=0
Ice.Trace.Network=0
Ice.Trace.Protocol=0
Ice.MessageSizeMax=20004800
